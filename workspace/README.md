# Workspace

Application source code lives here.

The devcontainer opens terminals in this folder by default. Repository infrastructure such as `.devcontainer/` and `scripts/` lives one level up at the repository root.
